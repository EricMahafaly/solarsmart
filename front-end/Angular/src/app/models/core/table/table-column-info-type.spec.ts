import { TableColumnInfoType } from './table-column-info-type';

describe('TableColumnInfoType', () => {
  it('should create an instance', () => {
    expect(new TableColumnInfoType()).toBeTruthy();
  });
});
